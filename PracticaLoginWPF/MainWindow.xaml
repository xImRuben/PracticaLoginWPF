<Window x:Class="PracticaLoginWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nexus Launcher" Height="550" Width="950"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        MouseDown="Window_MouseDown"
        Icon="/Assets/logo.ico">

    <Border CornerRadius="15" Background="{StaticResource BgDark}" BorderBrush="#2A2A35" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Color="#000000" ShadowDepth="10" Opacity="0.6"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.1*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" ClipToBounds="True">
                <Path Data="M0,0 L500,0 L350,600 L0,600 Z" Fill="{StaticResource BgLight}"/>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Image Source="/Assets/logo.ico" Width="80" Height="80" Margin="0,0,0,20"/>
                    <TextBlock Text="NEXUS" Foreground="White" FontSize="48" FontWeight="Black"  HorizontalAlignment="Center"/>
                    <TextBlock Text="CONNECTING WORLDS" Foreground="{StaticResource AccentColor}" FontSize="12" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Column="1" Margin="50">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="—" Foreground="{StaticResource TextSecondary}" Background="Transparent" BorderThickness="0" Width="30" Click="BtnMinimizar_Click" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
                    <Button Content="✕" Foreground="{StaticResource TextSecondary}" Background="Transparent" BorderThickness="0" Width="30" Click="BtnSalir_Click" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" VerticalAlignment="Center">
                    <TextBlock Text="Iniciar Sesión" FontSize="26" FontWeight="Bold" Foreground="White" Margin="0,0,0,30"/>

                    <TextBlock Text="ID DE USUARIO" Foreground="{StaticResource TextSecondary}" FontSize="10" FontWeight="Bold" Margin="2,0,0,8"/>
                    <TextBox x:Name="txtUsuario" Height="50" Style="{StaticResource NexusInputStyle}" Margin="0,0,0,20" VerticalContentAlignment="Center"/>

                    <TextBlock Text="CLAVE DE ACCESO" Foreground="{StaticResource TextSecondary}" FontSize="10" FontWeight="Bold" Margin="2,0,0,8"/>
                    <PasswordBox x:Name="txtPassword" Height="50" Style="{StaticResource NexusInputStyle}" Margin="0,0,0,15" VerticalContentAlignment="Center"/>

                    <CheckBox x:Name="chkRecordar" Content="Recordar mi usuario" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,25" Background="{StaticResource BgInput}" BorderBrush="#333">
                        <CheckBox.Resources>
                            <Style TargetType="CheckBox">
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Style>
                        </CheckBox.Resources>
                    </CheckBox>

                    <Button x:Name="btnLogin" Content="ENTRAR" Height="55" Click="BtnLogin_Click"
                            Foreground="White" FontSize="14" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#651FFF" Offset="0"/>
                                <GradientStop Color="#4527A0" Offset="1"/>
                            </LinearGradientBrush>
                        </Button.Background>
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                        <Button.Effect>
                            <DropShadowEffect Color="#651FFF" BlurRadius="15" ShadowDepth="0" Opacity="0.4"/>
                        </Button.Effect>
                    </Button>

                    <Button Content="¿No tienes cuenta? Regístrate aquí" Click="BtnIrRegistro_Click"
                            Foreground="{StaticResource TextSecondary}" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,15,0,0" FontSize="12"/>

                    <TextBlock x:Name="lblMensaje" Text="" Foreground="#FF5252" Margin="0,20,0,0" HorizontalAlignment="Center" TextWrapping="Wrap"/>
                </StackPanel>

                <TextBlock Grid.Row="2" Text="v3.0 Ultimate" Foreground="#444" FontSize="10" HorizontalAlignment="Center"/>
            </Grid>
        </Grid>
    </Border>
</Window>